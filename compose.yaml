services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-crm_db}
      POSTGRES_USER: ${POSTGRES_USER:-crm_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-crm_pass}
    healthcheck:
      test: ["CMD", "pg_isready", "-d", "${POSTGRES_DB:-crm_db}", "-U", "${POSTGRES_USER:-crm_user}"]
      timeout: 5s
      retries: 5
    volumes:
      - pgdata:/var/lib/postgresql/data:rw

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "pgsql://${POSTGRES_USER:-crm_user}:${POSTGRES_PASSWORD:-crm_pass}@postgres:5432/${POSTGRES_DB:-crm_db}"
    volumes:
      - ./backend:/app:rw
    depends_on:
      postgres:
        condition: service_healthy

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend:/app:rw
  #   depends_on:
  #     backend:
  #       condition: service_started

volumes:
  pgdata:

